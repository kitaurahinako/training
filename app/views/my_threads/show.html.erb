<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @my_thread.name %>
</p>

<p>
  <strong>Overview:</strong>
  <%= @my_thread.overview %>
</p>

<h1>Listing Comments</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comments.each do |comment| %>
      <tr>
        <td><%= comment.content %></td>
      <!--  <td><%= link_to 'Show', comment %></td> -->
        <td><%= link_to 'Edit', edit_comment_path(comment) %></td>
        <td><%= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= render partial: "comments/form", locals: {my_thread: @my_thread, comment: @comment}%>



<!-- <%= link_to 'New Comment', new_my_thread_comment_path(:my_thread_id) %> -->
<br>
<%= link_to 'Edit', edit_my_thread_path(@my_thread) %> |
<%= link_to 'Back', my_threads_path %>
